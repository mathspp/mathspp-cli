date: {{ date }}
metadata:
    author: Rodrigo Girão Serrão
    description: "{{ description }}"
    og:image: "https://mathspp.com/blog/{{ site_prefix }}{{ slug }}/thumbnail.png"
    twitter:image: "https://mathspp.com/blog/{{ site_prefix }}{{ slug }}/thumbnail.png"
slug: "{{ slug }}"
taxonomy:
    category:{% for category in categories %}
        - {{ category }}{% endfor %}
    tag:{% for tag in tags %}
        - {{ tag }}{% endfor %}
title: {{ title }}
